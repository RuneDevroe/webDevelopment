const setup = () => {

    var names = 'Doc,Dopey,Bashful,Grumpy,Sneeuwwitje,Sneezy,Sleepy,Happy';
    var nameArr = names.split(',');
    console.log(nameArr);
    let stuk;

    for (let i = 0; i < list.length; i++) {
        list[i]
        if(list[i].lastIndexOf('(P)') >=0 ){
            let String = list[i];
            let
                }else{
            if()
                let string = list[i]+' -- '
        }
    }
}
}
window.addEventListener('load', setup);
